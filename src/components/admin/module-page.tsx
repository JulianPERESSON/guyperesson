import Link from "next/link";
import { ArrowRight, Plus } from "lucide-react";
export function AdminModulePage({eyebrow,title,intro,metrics,rows,actionLabel,actionHref}:{eyebrow:string;title:string;intro:string;metrics?:Array<[string,string]>;rows:Array<Array<string>>;actionLabel?:string;actionHref?:string}){return <div><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="eyebrow text-[#a35f3f]">{eyebrow}</p><h1 className="mt-3 text-4xl">{title}</h1><p className="mt-3 max-w-2xl text-sm leading-6 text-stone-600">{intro}</p></div>{actionLabel&&actionHref&&<Link className="btn-primary" href={actionHref}><Plus size={16}/>{actionLabel}</Link>}</div>{metrics&&<div className="mt-7 grid gap-3 sm:grid-cols-3">{metrics.map(([label,value])=><div className="surface p-5" key={label}><p className="text-xs text-stone-500">{label}</p><p className="mt-2 text-2xl font-bold text-[#173f35]">{value}</p></div>)}</div>}<div className="surface mt-7 overflow-hidden"><div className="flex items-center justify-between border-b hairline px-5 py-4"><h2 className="font-sans text-sm font-bold">Aperçu récent</h2><label><span className="sr-only">Recherche rapide</span><input className="field min-h-9 w-44 py-1.5 text-xs" placeholder="Recherche rapide…"/></label></div><div className="divide-y hairline">{rows.map((row,index)=><div className="grid gap-2 px-5 py-4 text-sm sm:grid-cols-[1.3fr_1fr_.8fr_auto] sm:items-center" key={`${row[0]}-${index}`}><strong>{row[0]}</strong><span className="text-stone-500">{row[1]??"—"}</span><span className="text-stone-500">{row[2]??"—"}</span><button className="inline-flex items-center gap-1 text-xs font-bold text-[#173f35]">Voir <ArrowRight size={13}/></button></div>)}</div>{rows.length===0&&<p className="p-10 text-center text-sm text-stone-500">Aucune donnée pour le moment.</p>}</div><p className="mt-4 text-[11px] leading-5 text-stone-500">Les écrans d’administration utilisent les données de démonstration. Branchez l’adaptateur Prisma pour persister les modifications en production.</p></div>}
