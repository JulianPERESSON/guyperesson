import { ProductGrid } from "@/components/catalogue/product-grid";
import { Breadcrumb } from "@/components/ui/breadcrumb";
import type { CatalogProduct } from "@/types/catalog";
export function CuratedListing({ title, eyebrow, intro, products }: { title: string; eyebrow: string; intro: string; products: readonly CatalogProduct[] }) { return <><section className="border-b hairline bg-[#e9dfd0]/45"><div className="container-page py-10 sm:py-16"><Breadcrumb items={[{label:title}]} /><p className="eyebrow mt-10 text-[#a35f3f]">{eyebrow}</p><h1 className="mt-4 text-balance text-5xl leading-tight sm:text-7xl">{title}</h1><p className="mt-5 max-w-2xl leading-7 text-stone-600">{intro}</p></div></section><section className="container-page section-space"><div className="mb-8 flex items-center justify-between border-b hairline pb-4"><p className="text-sm text-stone-600"><strong className="text-stone-900">{products.length}</strong> objet{products.length > 1 ? "s" : ""}</p></div><ProductGrid products={products}/></section></>; }
