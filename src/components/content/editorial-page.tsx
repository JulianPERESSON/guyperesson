import { Breadcrumb } from "@/components/ui/breadcrumb";

export type EditorialSection = { title: string; paragraphs?: string[]; bullets?: string[] };

export function EditorialPage({ eyebrow = "Informations", title, intro, sections, updatedAt }: { eyebrow?: string; title: string; intro: string; sections: EditorialSection[]; updatedAt?: string }) {
  return <><section className="border-b hairline bg-[#e9dfd0]/55"><div className="container-page py-10 sm:py-16"><Breadcrumb items={[{ label: title }]} /><p className="eyebrow mt-10 text-[#a35f3f]">{eyebrow}</p><h1 className="mt-4 max-w-4xl text-balance text-4xl leading-[1.05] sm:text-6xl">{title}</h1><p className="mt-6 max-w-2xl text-base leading-7 text-stone-600 sm:text-lg">{intro}</p>{updatedAt && <p className="mt-5 text-xs text-stone-500">Dernière mise à jour : {updatedAt}</p>}</div></section><div className="container-page grid gap-10 py-14 lg:grid-cols-[15rem_minmax(0,48rem)] lg:justify-center lg:py-20"><aside className="hidden lg:block"><p className="eyebrow text-stone-500">Sur cette page</p><ol className="mt-5 space-y-3 border-l hairline pl-4 text-sm">{sections.map((section, index) => <li key={section.title}><a href={`#section-${index + 1}`} className="text-stone-600 hover:text-[#173f35]">{section.title}</a></li>)}</ol></aside><article className="space-y-12">{sections.map((section, index) => <section id={`section-${index + 1}`} key={section.title} className="scroll-mt-32"><h2 className="text-2xl sm:text-3xl">{section.title}</h2><div className="mt-4 space-y-4 text-[15px] leading-7 text-stone-650">{section.paragraphs?.map((paragraph) => <p key={paragraph}>{paragraph}</p>)}{section.bullets && <ul className="list-disc space-y-2 pl-5 marker:text-[#a35f3f]">{section.bullets.map((bullet) => <li key={bullet}>{bullet}</li>)}</ul>}</div></section>)}</article></div></>;
}
