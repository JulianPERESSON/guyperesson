"use client";
import Link from "next/link";
import { Check, Download, PackageCheck } from "lucide-react";
import { useEffect } from "react";
import { useShop } from "@/components/providers/shop-provider";
export function OrderConfirmationClient({sessionId,mode}:{sessionId:string;mode?:string}){const{clearCart}=useShop();useEffect(()=>clearCart(),[clearCart]);const number=`INV-${sessionId.replace(/[^a-zA-Z0-9]/g,"").slice(-8).toUpperCase()}`;return <section className="container-page grid min-h-[68vh] place-items-center py-16"><div className="surface w-full max-w-2xl p-7 text-center sm:p-12"><span className="mx-auto grid h-20 w-20 place-items-center rounded-full bg-emerald-100 text-emerald-900"><Check size={34}/></span><p className="eyebrow mt-7 text-[#a35f3f]">Commande {mode==="demo"?"de démonstration ":""}confirmée</p><h1 className="mt-4 text-5xl sm:text-6xl">Merci pour votre confiance.</h1><p className="mx-auto mt-5 max-w-lg leading-7 text-stone-600">Un récapitulatif sera envoyé par e-mail. Nous préparons chaque objet avec l’attention qu’il mérite.</p><div className="mx-auto mt-8 grid max-w-md grid-cols-2 gap-px overflow-hidden rounded-xl border hairline bg-stone-200 text-left text-sm"><div className="bg-white p-4"><p className="text-xs text-stone-500">Numéro</p><p className="mt-1 font-bold">{number}</p></div><div className="bg-white p-4"><p className="text-xs text-stone-500">Statut</p><p className="mt-1 font-bold">{mode==="demo"?"Simulation":"Payée"}</p></div></div>{mode==="demo"&&<p className="mx-auto mt-5 max-w-lg rounded-xl bg-amber-50 p-4 text-xs leading-5 text-amber-900"><strong>Mode démonstration :</strong> aucun paiement n’a été effectué et aucun stock réel n’a été modifié. Configurez Stripe et PostgreSQL pour la production.</p>}<div className="mt-8 flex flex-wrap justify-center gap-3"><Link className="btn-primary" href="/compte/commandes"><PackageCheck size={17}/>Suivre la commande</Link><button className="btn-secondary" type="button" onClick={()=>window.print()}><Download size={17}/>Imprimer</button></div></div></section>}
